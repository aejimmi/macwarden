[profile]
name = "paranoid"
description = "Deny-default — nothing runs unless explicitly listed"

[rules]
deny = ["*"]
allow = []

[rules.categories]
core-os = "allow"

[enforcement]
action = "kill"
exec_policy = "deny"
