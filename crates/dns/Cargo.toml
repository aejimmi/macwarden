[package]
name = "dns"
description = "Passive DNS cache and wire parser for macwarden hostname enrichment"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
thiserror = { workspace = true }
